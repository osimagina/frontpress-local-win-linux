# Editor

Set a favorite code editor once, then open any site's folder in it with a click.

## Choose your editor

<img width="2184" height="1664" alt="Screenshot 2026-06-05 at 11 42 38" src="https://github.com/user-attachments/assets/05945c13-61c8-423f-a51e-e2133eeb5361" />

<img width="2184" height="1664" alt="Screenshot 2026-06-05 at 11 42 41" src="https://github.com/user-attachments/assets/02d7d17a-8b6c-4dfc-ad80-ae6c6cfb569a" />


Open **Settings → Editor**.

The dropdown **auto‑detects editors installed** on your machine, including:

- Visual Studio Code (`code`)
- Cursor (`cursor`)
- VSCodium (`codium`)
- Zed
- Sublime Text (`subl`)
- PhpStorm / WebStorm
- Notepad++ (Windows)
- GNOME Text Editor / Kate / Gedit (Linux)

Pick one and it's saved immediately.

### Not in the list?

Choose **Other…** and type a **command** (e.g. `code`, `cursor`, `subl`).

Then click **Save**.

## Open a site in the editor

From a site's **⋯ menu → Open in editor**.

This opens the site's **`site/` folder** — your editable content, themes, and `config.json` — rather than the whole FrontPress framework, so you land exactly where you do your work. (If a brand‑new site hasn't created its `site/` folder yet, it falls back to the site root.)

If you haven't picked an editor, the app will prompt you to set one in Settings first.

---

Next: **[Sites location & sync →](syncing.md)**
